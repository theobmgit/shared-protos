# Shared Protocol Buffers

Shared Protocol Buffer definitions for Expense Tracker services. This repository contains only `.proto` files - each consuming service generates code during their build process.
